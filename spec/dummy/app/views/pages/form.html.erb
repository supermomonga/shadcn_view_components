<main class="form-page">
  <h1>Form</h1>
  <section id="subscribe-form">
    <% if notice %>
      <p id="form-notice"><%= notice %></p>
    <% end %>
    <%= form_with url: pages_form_path, method: :post, local: true do %>
      <% if @email_error %>
        <%= render(Shadcn::Alert.new(variant: :destructive)) do %>
          <%= render(Shadcn::Alert::Title.new) { "送信できませんでした" } %>
          <%= render(Shadcn::Alert::Description.new) { @email_error } %>
        <% end %>
      <% end %>
      <%= render(Shadcn::Form::Item.new(invalid: @email_error.present?)) do %>
        <%= render(Shadcn::Field::Label.new(for: "subscribe-email")) { "メールアドレス" } %>
        <%= render(Shadcn::Input.new(
          id: "subscribe-email",
          name: "email",
          type: "text",
          aria: { invalid: @email_error.present?.to_s }
        )) %>
        <%= render(Shadcn::Field::Description.new) { "ニュースレターの送信先です" } %>
        <%= render(Shadcn::Form::Error.new(id: "email-message", message: @email_error)) %>
      <% end %>
      <%= render(Shadcn::Button.new(type: "submit")) { "登録する" } %>
    <% end %>
  </section>
</main>
